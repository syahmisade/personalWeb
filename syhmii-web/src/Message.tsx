// Example to begin with
// PascalCasing
function Message() {
  // JSX: JavaScript XML
  const name = "syhmiisade";
  if (name) return <h1>Hello {name}</h1>;
  //Kalau name tak contain apa2
  return <h1>Hello Bro</h1>;
}

// Jangan lupa semi colon
export default Message;
